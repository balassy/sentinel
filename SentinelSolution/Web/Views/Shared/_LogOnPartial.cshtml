@using Sentinel.Web.Resources.Shared

@using (Html.BeginForm( MVC.Home.LogOff(), FormMethod.Post, new { id = "logoutForm" }))
{
	@Html.AntiForgeryToken()
	<a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-default navbar-btn pull-right">@LogOnPartialRes.LogOff</a>
}

